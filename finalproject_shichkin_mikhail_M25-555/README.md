# linux_practicum

# Проект "Платформа для отслеживания и симуляции торговли валютами"

## Описание проекта
Проект представляет собой комплексную консольную платформу для управления виртуальными валютными портфелями, позволяющая пользователям работать с фиатными валютами и криптоактивами в единой системе.

### Ключевые возможности платформы:

1. **Управление пользователями**
   - Регистрация и аутентификация с безопасным хранением паролей
   - Персонализированные учетные записи с историей операций

2. **Виртуальные портфели**
   - Создание и управление мультивалютными кошельками
   - Поддержка фиатных валют (USD, EUR, RUB) и криптовалют (BTC, ETH)
   - Реальное отражение балансов в выбранной базовой валюте

3. **Торговые операции**
   - Покупка и продажа активов по текущим рыночным курсам
   - Автоматическое создание кошельков для новых валют
   - Контроль достаточности средств для операций

4. **Актуальная аналитика**
   - Отслеживание курсов в реальном времени
   - Расчет общей стоимости портфеля

### Архитектурные особенности:

**Двухсервисная архитектура:**
- **Core Service** — основной сервис с пользовательским интерфейсом (CLI), управлением пользователями, портфелями и транзакциями
- **Parser Service** — независимый сервис для сбора и обновления курсов валют с публичных API

**Гибкость и масштабируемость:**
- Модульная структура с четким разделением ответственности
- JSON-хранилище данных для простоты разработки
- Возможность расширения списка поддерживаемых валют

## Технологический стек

### Основные инструменты:
- **Poetry** - управление зависимостями, сборка и публикация пакетов
- **Ruff** - линтинг и форматирование кода
- **Git** - контроль версий с применением Gitflow/GitHub Flow
- **Makefile** - автоматизация рутинных задач

## Установка и запуск

### Предварительные требования:
- Python 3.8+
- Poetry
- Git

### Установка:

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Shichkin9/finalproject_Shichkin_Mikhail_M25-555.git
cd finalproject_Shichkin_Mikhail_M25-555
```

2. Установите зависимости:
```bash
poetry install
```

3. Запустите приложение:
```bash
poetry run project
```

## Основные команды CLI

| Команда | Описание | Пример |
|----------|-----------|---------|
| `register --username <имя> --password <пароль>` | Регистрация нового пользователя | `register --username test --password 1234` |
| `login --username <имя> --password <пароль>` | Авторизация пользователя | `login --username test --password 1234` |
| `buy --currency <код> --amount <число>` | Покупка валюты по текущему курсу | `buy --currency BTC --amount 0.05` |
| `sell --currency <код> --amount <число>` | Продажа валюты | `sell --currency BTC --amount 0.02` |
| `get-rate --from <валюта> --to <валюта>` | Получить актуальный курс валюты | `get-rate --from BTC --to USD` |
| `show-portfolio --base <валюта>` | Показать портфель пользователя в выбранной базе | `show-portfolio --base USD` |
| `update-rates` | Обновить курсы валют (Parser Service) | `update-rates` |
| `show-rates` | Показать кэшированные курсы из `rates.json` | `show-rates` |
| `exit` | Завершить работу приложения | `exit` |

## Файл Makefile

Файл Makefile заполнен, но на Windows не получилось его реализовать, поэтому все команды выполнялись через явное их указание.

```makefile
install:
    poetry install

project:
    poetry run project

build:
    poetry build

publish:
    poetry publish --dry-run

package-install:
    python3 -m pip install dist/*.whl

lint:
    poetry run ruff check .
```

## Вход в приложение

Сначала необходимо войти в приложение с помощью команды:
```bash
poetry run project
```

После запуска следуйте инструкциям в консоли для регистрации или входа в систему.

## Структура проекта

```
finalproject_Shichkin_Mikhail_M25-555/
├── src/
│   └── project/          # Основной пакет
│       ├── __init__.py
│       ├── main.py       # Точка входа
│       ├── core/         # Core Service
│       │   ├── __init__.py
│       │   ├── user_manager.py
│       │   ├── portfolio_manager.py
│       │   └── transaction_manager.py
│       └── parser/       # Parser Service
│           ├── __init__.py
│           └── rate_fetcher.py
├── data/                 # JSON хранилища
│   ├── users.json
│   ├── portfolios.json
│   └── rates.json
├── tests/               # Тесты
├── pyproject.toml      # Конфигурация Poetry
├── Makefile           # Автоматизация
└── README.md          # Этот файл
```

## Разработка

### Запуск тестов:
```bash
poetry run pytest
```

### Проверка стиля кода:
```bash
poetry run ruff check .
```

### Сборка пакета:
```bash
poetry build
```

## Лицензия
Этот проект создан в рамках учебного задания.
😊
